# baby-ehon E2E テスト依存（npm は使わず child-review と同じ Python+Playwright に揃える）
# 実行例:
#   uv run --with pytest-playwright playwright install chromium
#   uv run --with pytest-playwright python -m pytest e2e/ -q
pytest>=8.0
pytest-playwright>=0.5
# a11y 自動チェック（axe-core を同梱。CDN 非依存でオフライン実行できる）
axe-playwright-python>=0.1.7
